# Text Tools

This category contains text processing and JSON utilities.

## Subfolders

- `klen-json/` — JSON-related processing tools.
- `setxt/` — enhanced text manager scripts.

## Recommended usage

Use `banshee.rb` for common text operations such as duplicate line removal, parentheses extraction, prefix insertion, and URL cleaning. The scripts here are helpful for more specialized text workflows.